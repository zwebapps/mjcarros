FROM node:18-bullseye
WORKDIR /app

# Install dependencies first for caching
COPY package*.json ./
RUN npm ci

# Copy the rest
COPY . .

# Generate Prisma client
RUN npx prisma generate

ENV HOST=0.0.0.0
ENV PORT=3000
EXPOSE 3000

CMD ["bash","-lc","npx next dev -H 0.0.0.0 -p 3000"]
